;; vi: ft=lisp

(defsrc
  caps
  esc
)

(defalias
    bye A-f4
) 

(defalias 
 	escctrl (tap-hold 200 250 esc lctl))

(defalias 
  	usefulesc (tap-hold 100 200 caps @bye)
)

(deflayer base
	@escctrl
	@usefulesc
)
